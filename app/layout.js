export const metadata = {
  title: 'Haircutting-VLA Benchmark',
  description: 'Haircutting-VLA benchmark site',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
